---
controller_roles:
{% for users_item in users_list.list %}
  - user: "{{ users_item['gitlab.com User'] | regex_replace('@','') }}"
    job_templates:
      - "{{ orgs | default('superadmin') }} JT_Gitlab_Webhook_Creation"
      - "{{ orgs | default('superadmin') }} JT_Gitlab_Project_Creation"
    role: execute
{% endfor %}
...
