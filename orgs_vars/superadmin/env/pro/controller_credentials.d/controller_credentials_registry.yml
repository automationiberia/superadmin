---
controller_credentials:
  - name: "{{ orgs }} {{ env }} {{ controller_location }} Automation Private Hub Container Registry"
    description: "Credential to connect to Container Registry at AtomationHub Private"
    credential_type: "Container Registry"
    organization: "{{ orgs }}"
    inputs:
      username: "sa-pro-site1-download"
      password: "Password-site1"
      host: lb-hub-pro-site1.lab.example.com
      verify_ssl: true
    controller_location: site1

  - name: "{{ orgs }} {{ env }} {{ controller_location }} Automation Private Hub Container Registry"
    description: "Credential to connect to Container Registry at AtomationHub Private"
    credential_type: "Container Registry"
    organization: "{{ orgs }}"
    inputs:
      username: "sa-pro-site2-download"
      password: "Password-site2"
      host: lb-hub-pro-site2.lab.example.com
      verify_ssl: true
    controller_location: site2
...
